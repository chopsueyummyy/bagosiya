name: riasec_app
description: "RIASEC Career Assessment Web Application - Phase 1 Frontend Proposal"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.1.0
  provider: ^6.1.1
  intl: ^0.20.2
  flutter_svg: ^2.0.9
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
